import 'package:flutter/material.dart';

class AppColors extends StatelessWidget
{
    // App Colors //
    static Color primary = Color(0xFF44086A);
    static Color secundary = Color(0xFF5C309E);
    static Color tertiary = Color(0xFFB6086C);

    static Color contraster_normal = Color(0xFF0086F6);
    static Color contraster_dark = Color(0xFF0858C4);

    static Color card_background = Color(0xFFEAEAF4);
    static Color card_shadow = Color(0x2A000000);

    static Color app_bar_clicked = Color(0xFFFFFFFF);
    static Color app_bar_noclicked = Color(0xFF300458);

    static Color text_strong = Color(0xFF403844);
    static Color text_normal = Color(0xFF4C4C4C);
    static Color text_light = Color(0xFF909090);
    static Color text_white = Color(0xFFFFFFFF);

    static Color software_normal = Color(0xFF963CBC);
    static Color software_dark = Color(0xFF6F308A);

    static Color design_normal = Color(0xFF32DE32);
    static Color design_dark = Color(0xFF18B818);

    static Color music_normal = Color(0xFF60CCEA);
    static Color music_dark = Color(0xFF00B4E0);

    static Color audiovisual_normal = Color(0xFFC20018);
    static Color audiovisual_dark = Color(0xFF7C0018);

    @override
    Widget build(BuildContext context)
    {
        return AppColors();
    }
}

class AppGradients extends StatelessWidget
{
    AppGradients({Key key}) : super(key: key);

    @override
    Widget build(BuildContext context)
    {
        return null;
    }
}